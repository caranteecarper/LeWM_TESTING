# 可视化

本页展示最佳 `h_q_action / small_mlp_128 / seed=2` 的 C head。

C 模块预测的是 official predictor failure risk：

- 不是环境 reward；
- 不是 episode continuation；
- 不是 SIGReg loss；
- 不是已经接入 LeWM 的修正模块。

输出图片目录：`/data/lzt26/lewm_official_tworoom_readout_git/outputs/tworoom_failure_confidence_head_v1/figures`
